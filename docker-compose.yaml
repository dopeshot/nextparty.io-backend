version: "3.7"
services:
  backend:
    container_name: wop-backend
    image: coffeemakingtoaster/wop-backend:latest
    restart: always
    ports:
      - "3001:3000"
    links:
      - mongo
    depends_on:
      - mongo
    
  mongo:
    image: mongo:5
    restart: always
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"
    logging:
      driver: "none"

  
             
